<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>
  <h6><%=pluralize( post.views, "view") %></h6>
  <h4>posted by <%= post.user.email %></h4>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
